# =============================================================================
# Variáveis de ambiente da stack VPS (docker-compose.vps.yml)
# Domínio: https://gohelpdesk.ichatbot.com.br
# =============================================================================

# ─── Imagem Docker ───────────────────────────────────────────────────────────
# Apenas a versão/tag da imagem. O nome do repositório é fixo no compose.
DOCKERHUB_IMAGE=1.0.10

# ─── Banco de dados (Postgres) ───────────────────────────────────────────────
POSTGRES_DB=helpdesk
POSTGRES_USER=helpdesk
# ATENÇÃO: se o volume db_data já existia na VPS, use a SENHA ANTIGA aqui,
# senão o app não conecta. Só use a senha gerada abaixo se o banco for novo.
POSTGRES_PASSWORD=GnydzJXq2uAATrpneDad7poVn1vwALcd

# ─── Segredos da aplicação ───────────────────────────────────────────────────
# Trocar estes apenas invalida sessões/tokens atuais (usuários re-logam). Sem perda de dados.
SESSION_SECRET=fRj5F7datod63iwAwPjWI3sL/JMqYl5DsqX4OX7IbFQ=
JWT_SECRET=UJDgakobf8lIrFGJbP21g1ezbdHKbTjpzghAbj27GJc=

# ─── SMTP (opcional — notificações por e-mail) ───────────────────────────────
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

# ─── Feature flags (opcional) ────────────────────────────────────────────────
GUEST_SUBMISSION_ENABLED=false
SLA_ENABLED=false
MFA_ENABLED=false

# ─── Log ─────────────────────────────────────────────────────────────────────
LOG_LEVEL=info
